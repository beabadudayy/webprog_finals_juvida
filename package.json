{
  "name": "webprog-finals-juvida",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd my-profile && npm install && cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.95.3"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
